fastapi==0.141.1
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.15.0
pydantic_core==2.46.4
pytest==9.1.1
uvicorn==0.52.1
# --- Persistance + auth (socle) ---
sqlalchemy==2.0.36
alembic==1.14.0
# Driver PostgreSQL (production). L'extra [binary] embarque libpq : aucune
# dépendance système à installer côté hébergeur.
psycopg[binary]==3.2.9
PyJWT==2.10.1
bcrypt==4.2.1
email-validator==2.2.0
# --- Optimisation de portefeuille (allocation par classes d'actifs) ---
# Utilisées sur le chemin d'une requête (HRP/ERC + covariance). Pas d'accès réseau.
numpy==2.2.6
scipy==1.15.3
pandas==2.2.3
